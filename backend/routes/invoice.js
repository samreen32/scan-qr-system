const express = require("express");
const router = express.Router();
const { body, validationResult } = require("express-validator");

/****** 1st Route *******/
// Create an Invoice using: POST "api/auth/createInvoice". No login required.


module.exports = router;